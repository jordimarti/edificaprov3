<%= form_with(model: video_rating) do |form| %>
  <% if video_rating.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(video_rating.errors.count, "error") %> prohibited this video_rating from being saved:</h2>

      <ul>
        <% video_rating.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :video_id %>
    <%= form.text_field :video_id %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
